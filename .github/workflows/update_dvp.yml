name: Legacy DvP workflow (disabled)

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This Cloudflare KV/Sleeper workflow has been retired."
          echo "Use .github/workflows/refresh-data.yml for league-scoring nflverse datasets."
