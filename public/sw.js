const VERSION='fcc-claim-sync-20260925';const SHELL=['/','/index.html','/styles.css','/app.js','/ui.js','/waiver-ui.js','/forecast-ui.js','/claim-plan-ui.js','/claim-outcome-ui.js','/waiver-activity-ui.js','/claim-plan.css','/teams.json','/reliability.css','/team-colors.css','/manifest.webmanifest','/icon.svg'];self.addEventListener('install',event=>event.waitUntil(caches.open(VERSION).then(cache=>cache.addAll(SHELL)).then(()=>self.skipWaiting())));self.addEventListener('activate',event=>event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key!==VERSION).map(key=>caches.delete(key)))).then(()=>self.clients.claim())));self.addEventListener('fetch',event=>{const request=event.request,url=new URL(request.url);if(request.method!=='GET'||url.pathname.startsWith('/api/'))return;event.respondWith((async()=>{try{const response=await fetch(request);if(response.ok&&url.origin===self.location.origin)(await caches.open(VERSION)).put(request,response.clone());return response}catch{return await caches.match(request)||request.mode==='navigate'&&await caches.match('/')||Response.error()}})())});
